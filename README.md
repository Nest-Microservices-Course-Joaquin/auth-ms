## Dev

1. Clonar el proyecto
2. Crear un archivo `.env` basado en el `.env.template`
3. Levantar el proyecto con `pnpm run start:dev`

## Prod

1. Ejecutar el comando
````
docker build -f dockerfile.prod -t auth-ms .
````

